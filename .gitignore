# ── OS noise ─────────────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# ── Editor / IDE ─────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Secrets — never commit these to a public repository ──
# The Telegram bot token and chat id live in Cloudflare Worker secrets,
# NOT in this repo. Anything matching the patterns below is a mistake.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets.json
credentials.json
config.local.js
wrangler.toml
.dev.vars

# ── Build / tooling artefacts (site itself has no build step) ─
node_modules/
dist/
build/
.cache/
*.log
npm-debug.log*
